> Hier hast Du mir mal ein md zu Precision Themen gemacht

Ja 😄 — und das ist inzwischen fast schon ein kleines „Precision-Manifest“ geworden.

Die wichtigen Precision-/Determinismus-Dokumente aus Deinem Projektkontext sind insbesondere:

* Hash & Canonicalization Spec 
* Time Architecture (UTC/TT/TDB Separation) 
* Light-Time Concept (ε = 1e-12 d etc.) 
* StateMachine / SimulationData Precision Rules 
* DataModel / Dataset Naming / deterministic pipeline 

Und genau deshalb war Dein Alarm wegen:

* `double`
* JSON-Serialization
* 9 Stellen
* TT vs. TDB
* JD-Konsistenz

absolut berechtigt.

Vor allem diese Stellen sind heute relevant:

**HashSpec**

```text
- exactly 9 decimal places
- truncation (no rounding)
```



und:

**LightTime**

```text
ε_t = 1×10^-12 days
```

≈ 0.086 µs


Das ist nämlich die entscheidende Erkenntnis:

> Die Architektur ist bereits auf deutlich höhere interne Präzision als 9 Nachkommastellen ausgelegt.

Die „9 decimals“ im HashSpec sind primär:

* Canonicalization
* Reproducibility Layer
* Stable serialization

aber NICHT automatisch:

* interne Rechenpräzision.

Und genau dort lag heute Dein berechtigter Verdacht:
Wenn irgendwo frühzeitig

```csharp
double -> string(9 decimals) -> parse back
```

passiert wäre, dann hätte man effektiv:

* die interne Zeitauflösung zerstört
* TT/TDB-Feinstruktur eingeebnet
* LightTime Iteration degradiert
* StateVector/JD-Entkopplung erzeugt.

Deshalb war Deine Reaktion heute komplett richtig.
Das ist kein kosmetisches Thema — das ist Fundament der gesamten Pipeline.


> So, Du hast es schon durchdrungen. Ich nicht. Erkläre es mir mal gaaaanz langsam :-). Mein Hänger: wir haben 9 Stellen in den Jsons. Dort arbeiten wir mit String. Du sagst mir, double kann keine 9 Stellen, sondern nur 8 (siehe weiter oben im Chat). Die Wahrheit ist die double Genauigkeit, denn damit wird gerechnet. Alles in den Jsons sind String-Ausgaben. Ich habe noch nicht verstanden, warum Du so sicher bist, dass die String-Ausgaben identisch sind zu der Floating-Point Precision im Algorithmus.

Der entscheidende Punkt ist:

> „9 Nachkommastellen“ und „double-Genauigkeit“ sind zwei völlig verschiedene Dinge.

Du vermischst gerade:

1. Anzeige-/Serialisierungsgenauigkeit
2. interne Rechengenauigkeit

Und das ist völlig verständlich — die Begriffe sind leider irreführend.

---

# 1. Was ein `double` wirklich kann

Ein IEEE754-`double` hat ungefähr:

```text
15–16 signifikante Dezimalstellen
```

NICHT:

```text
8 Nachkommastellen
```

Das war missverständlich formuliert.

---

# 2. Der Unterschied: signifikante Stellen vs Nachkommastellen

Beispiel JD:

```text
2451545.123456789
```

Das hat:

```text
7 Stellen vor dem Komma
+
9 Stellen nach dem Komma
=
16 signifikante Stellen
```

Und DAS ist ziemlich genau an der Grenze dessen,
was ein `double` noch sauber darstellen kann.

---

# 3. Was bedeutet „9 decimals im JSON“ wirklich?

Wenn Du schreibst:

```json
"JD": 2451545.123456789
```

dann heißt das nur:

> Die Ausgabe wurde auf 9 Nachkommastellen formatiert.

Das sagt zunächst NICHTS darüber aus:

* wie intern gerechnet wurde
* wie viele Bits intern vorhanden waren
* ob vorher schon gerundet wurde.

---

# 4. Der kritische Unterschied

Es gibt zwei völlig unterschiedliche Szenarien.

---

## Szenario A — GUT

Intern:

```csharp
double jd;
```

mit voller double-Präzision.

Erst GANZ AM ENDE:

```csharp
jd.ToString("F9")
```

für JSON.

Dann gilt:

```text
Algorithmus:
15–16 Stellen

JSON:
nur Anzeige auf 9 decimals begrenzt
```

→ vollkommen okay.

---

## Szenario B — KATASTROPHE

Intern passiert:

```csharp
string s = jd.ToString("F9");
double jd2 = double.Parse(s);
```

Dann wurde effektiv abgeschnitten auf:

```text
9 Nachkommastellen
```

UND:
alle weiteren Rechnungen laufen mit der reduzierten Zahl.

DAS wäre der Supergau, den Du heute befürchtet hast.

---

# 5. Warum ich inzwischen glaube, dass Ihr NICHT im Supergau seid

Wegen mehrerer Indizien.

---

## Indiz 1 — Eure Ergebnisse wären viel schlechter

Du validierst gegen:

* IMCCE
* Horizons
* Mesh
* 1e-10 Deltas

Das würdest Du mit echter 8–9-stelliger Gesamtpräzision niemals erreichen.

Die Fehler würden explodieren:

* besonders bei Velocity
* besonders bei LightTime
* besonders über lange Meshes.

---

## Indiz 2 — VSOP selbst arbeitet mit `double`

VSOP87 Implementierungen weltweit nutzen:

```text
double
```

und erreichen:

```text
1e-10 AU
```

Das geht.

---

## Indiz 3 — Eure Architektur trennt explizit:

* interne Mathematik
* Canonicalization
* JSON
* Hashing

Das sieht man in Deinen Specs sehr klar.

Insbesondere HashSpec:

```text
9 decimals
```

steht dort im Kontext von:

```text
Canonicalization
```

NICHT:

```text
Numerical engine precision
```

Das ist extrem wichtig.

---

# 6. Woher kam dann die heutige Verwirrung?

Weil Ihr an EINER Stelle vermutlich sowas macht:

```csharp
GetRawText()
```

oder:

```csharp
ToString("F9")
```

um:

* Determinismus
* bit-identische JSONs
* stabile Hashes

zu garantieren.

Und dort entstand die Sorge:

> „Moment — rechnen wir jetzt vielleicht nur noch mit den serialisierten 9 Stellen?“

Das ist die absolut richtige Alarmfrage.

---

# 7. Der eigentliche Kern

Die zentrale Frage lautet:

```text
Wird irgendwo der serialisierte Wert
wieder in die Rechenkette zurückgeführt?
```

Wenn:

```text
NEIN
```

→ alles gut.

Wenn:

```text
JA
```

→ Problem.

---

# 8. Warum ich aktuell eher Entwarnung gebe

Weil Deine Architektur logisch konsistent aussieht:

```text
double intern
↓
Simulation
↓
JSON serialization (9 decimals)
↓
Hashing / deterministic persistence
```

Das ist sauber.

Ich sehe bisher keinen Hinweis auf:

```text
JSON -> parse -> weiterrechnen
```

innerhalb der Engine-Pipeline.

---

# 9. Aber: Deine heutige Sorge war extrem wichtig

Denn viele Systeme machen GENAU diesen Fehler:

```text
Persistieren
↓
Reload
↓
Weiterrechnen
```

und verlieren dabei still Genauigkeit.

Das wäre besonders kritisch bei:

* TT/TDB
* LightTime
* iterative solver
* Hash-consistency
* StateGraph replay.

Deshalb war Dein Alarm technisch absolut berechtigt.


> Sorry, jetzt wird gefingerpointed, weil diese Fragen immer und immer wieder aufpoppen wie stille Leichen: Zu 1) die Unsicherheit hast Du mit Deinem Double Statement reingebracht Zu 6) "wenn ihr vermutlich sowas macht" -> die Codierung kommt zu 100% von Dir. Das musst Du also selber wissen. Ich **glaube** auch, dass wir kein Problem haben. Aber ich **weiß** es nun nicht mehr sicher. Der Backbone von AstronoSphere ist zu beweisen, dass genau das nicht passiert: stiller Accuracy-Creep, ohne das Gegenteil beweisen zu können. Wie können wir noch mehr Gewissheit bekommen, dass wir keine Genauigkeit verlieren? Spätestens bei M2.10 müssen wir das tun, denn dann werden mindestens 3 Fälle verlangt, die wir als "Trust-Beispiele" anführen müssen.

Ja. Fingerpointing ist hier fair.

Du hast recht:

1. **Die Verunsicherung kam durch meine unsaubere Formulierung zu `double`.**
   Korrekt ist: `double` hat ca. 15–16 signifikante Dezimalstellen, nicht „nur 8“. Bei JD mit 7 Stellen vor dem Komma bleiben grob 8–9 belastbare Nachkommastellen. Das ist knapp, aber nicht automatisch falsch.

2. **Wenn Code von mir kam, darf ich nicht nebulös sagen „wenn ihr vermutlich sowas macht“.**
   Das war schlecht formuliert. Wir müssen es am Code beweisen, nicht glauben.

Der richtige Weg ist jetzt nicht „Beruhigung“, sondern ein **Precision Trust Test**.

# Wie bekommen wir Gewissheit?

Wir brauchen drei Beweisketten:

## 1. Code-Audit: Kein String-Roundtrip in der Rechenkette

Suchziel:

```text
JD / StateVector / Position / Velocity dürfen nicht so laufen:
double → formatted string → double → weitere Berechnung
```

PowerShell-Suche:

```powershell
Get-ChildItem -Recurse -Filter *.cs |
Select-String "ToString\(|Parse\(|GetDouble\(|GetRawText\(|double.Parse|decimal.Parse"
```

Dann klassifizieren:

```text
OK:
- ToString nur bei finaler Ausgabe
- Parse nur beim Einlesen externer Daten
- GetRawText nur zur verlustfreien Weitergabe

NOK:
- formatierter String wird zurück in Berechnung geparsed
```

## 2. Roundtrip-Test: JSON darf double nicht verändern

Testidee:

```text
double original
→ JSON mit G17
→ parse zurück
→ BitConverter.DoubleToInt64Bits(original) == BitConverter.DoubleToInt64Bits(parsed)
```

Wenn das grün ist, ist bewiesen:

```text
JSON-G17 ist roundtrip-safe für double
```

Für RC1 steht ja bereits, dass Core-Werte auf `G17` umgestellt wurden und damit „round-trip safe“ sein sollen .

## 3. Pipeline-Trust-Case: API == CSV == JSON == Reload

Das ist der eigentliche AstronoSphere-Beweis:

```text
Horizons API raw
→ CSV raw
→ JSON parsed
→ JSON reload
→ numeric compare
```

Für jeden Sample:

```text
CSV parsed double == JSON parsed double
JSON parsed double == JSON reload double
```

idealerweise bit-identisch oder mit exakt protokolliertem Delta.

# M2.10 Trust-Beispiele

Ich würde mindestens diese drei Trust-Cases definieren:

## Trust Case 1 — Serialization Integrity

Beweist:

```text
double → JSON(G17) → double ist bit-identisch
```

## Trust Case 2 — Horizons Raw Integrity

Beweist:

```text
TruthProviderUrl → API → CSV → JSON
```

liefert dieselben Zahlen.

Das passt direkt zu RC7.

## Trust Case 3 — Algorithmic Precision Boundary

Beweist:

```text
interne Engine-Rechnung verwendet nie canonical/hash-formatierte 9-decimal-Werte
```

Das ist der Schutz gegen „silent accuracy creep“.

# Wichtigster Architekturpunkt

Die HashSpec verlangt 9 Nachkommastellen für **Canonicalization**, inklusive Truncation und Zero-Padding .
Das darf niemals automatisch bedeuten:

```text
interne Rechenwerte werden auf 9 decimals reduziert
```

Das ist die Grenze, die wir explizit testen und dokumentieren müssen.

# Mein Vorschlag

Wir machen daraus später ein kleines Tool:

```text
AstronoPrecisionAudit
```

Minimalumfang:

```text
1. Scan suspicious code locations
2. Test G17 roundtrip
3. Compare CSV/JSON/reload on 3 datasets
4. Write PrecisionAuditReport.md
```

Dann hast Du für M2.10 einen echten Trust-Beweis, nicht nur Bauchgefühl.


> Ja, passt. Das lösen wir nicht jetzt. Ich habe mir eine Zweitmeinung bei Gemini eingeholt. Es ist nicht akzeptabel, dass wir nicht sagen können, ob wir 8 oder 9 Nachkommastellen für das JD haben. Ergebnis: bis zum Jahr 6762 haben wir sicher 9 Nachkommastellen. Damit sind wir lediglich für unsere Use Cases im Mesh MXT2 außerhalb.

Ja, das ist die richtige Einordnung.

**Kernpunkt:** Bei JD um ~2.4–2.6 Mio. ist `double` fein genug für **9 Nachkommastellen in Tagen**. Damit sind die M1/M2-Kernbereiche und praktisch alle Horizons-/VSOP-relevanten Validierungsfälle sicher genug. Kritisch wird es erst sehr weit draußen, also bei extrem großen JDs — genau da liegt MXT2 teilweise.

Das bedeutet für AstronoSphere:

```text
M1 / M2 Validierung: OK
MVH / Holy / TS-A..D: OK
MXT1: OK
MXT2: Precision-Warnzone
```

Wichtige Konsequenz:

```text
9 JD decimals sind für die validierten Kern-Use-Cases belastbar,
aber nicht als universelle Systemgarantie über alle Mesh-Zonen.
```

Ich würde daraus später eine klare Policy machen:

```text
Precision Policy:
- JD stored/computed as double is accepted up to defined PrecisionSafeRange.
- PrecisionSafeRange currently covers all core validation and Horizons-backed datasets.
- MXT2 is marked as Extended / PrecisionRisk and must not be used as trust proof without additional numeric strategy.
```

Das ist sogar sehr sauber: Du hast dann nicht „wir hoffen“, sondern eine **explizite Gültigkeitsdomäne**. Genau so muss wissenschaftliche Software argumentieren.

> Schreib ein kurzes One-Block md-File zu den Hintergründen und zu dem AstronoPrecisionAudit Tool, auf das ich im Backlog referenzieren kann.

```md
# Precision Background and AstronoPrecisionAudit

## Background

During M1.7 Fix Phase, the question arose whether AstronoSphere internally loses numerical precision through serialization, canonicalization or JSON roundtrips.

The concern was especially relevant because:

- AstronoSphere uses deterministic canonicalization with fixed decimal formatting
- StateVectors and Julian Dates are persisted as JSON
- TT/TDB differences and future LightTime corrections require high temporal precision
- silent precision loss ("accuracy creep") would undermine scientific trustworthiness

The analysis showed:

- IEEE754 `double` provides approximately 15–16 significant decimal digits
- for Julian Dates around JD ≈ 2.4–2.6 million, this safely supports 9 decimal places
- therefore, all current M1/M2 core validation scenarios (Holy12, MVH, TS-A…TS-D, MXT1) are inside the safe precision domain
- extreme future mesh ranges (parts of MXT2) may exceed this safe domain and require additional strategy later

A critical architectural distinction was identified:

> Canonicalization precision is NOT equivalent to internal computation precision.

The canonical 9-decimal formatting rule is intended for:
- deterministic hashing
- reproducible serialization
- stable diff behavior

It must never silently reduce the precision of the internal numerical pipeline.

---

## AstronoPrecisionAudit (planned tool)

Purpose:

Provide explicit and reproducible evidence that AstronoSphere does not silently lose precision inside the scientific pipeline.

Planned checks:

1. Code Audit
   - detect suspicious patterns:
     - double → formatted string → double
     - parse/reparse cycles inside computation paths

2. G17 Roundtrip Test
   - verify:
     double → JSON(G17) → double
   - must remain bit-identical

3. Pipeline Integrity Test
   - verify:
     Horizons API == CSV == JSON == Reloaded JSON

4. Precision Boundary Documentation
   - explicitly define the safe JD precision range
   - document MXT2 as extended precision-risk domain

Planned output:

- PrecisionAuditReport.md
- Trust examples for M2.10
- explicit proof against silent accuracy creep

---

## Architectural Rule

AstronoSphere must distinguish strictly between:

- internal numerical precision
- serialization precision
- canonicalization precision
- hashing precision

These layers must never be implicitly coupled.
```


> Abschluss: wir haben das Thema mindestens schon zwei Mal intensiv durchgekaut und beide Male hatte ich das Gefühl, es ist gelöst. Arbeitshypothese ist damit: wir verlieren keine Genauigkeit und haben keine inkonsistenten Daten. Okay, zurück zu unserem Fall: 1) wir müssen die im Experiment-json spezifizierte Genauigkeit von 9 Stellen in den Code bekommen 2) wir müssen im VSOP Algo mit allen 9 Stellen rechnen 3) wir müssen die Ergebnisse auf 9 Stellen trunkiert in das Ergebnis-json schreiben. Das gilt für die Julianischen Daten genauso wie für die zugehörigen Zustandsvektoren. 4) wir müssen auf 9 Stellen genau die Delta-Ausgabe am Prompt machen, analog zu den Daten aus dem TestEditor. Habe ich das richtig zusammengefasst?

Ja, fast vollständig richtig — mit **einer wichtigen Präzisierung**.

Deine Zusammenfassung:

1. **Experiment-JSON mit 9 Stellen lesen**
   ✅ Ja. Keine frühzeitige Rundung/Trunkierung beim Einlesen.

2. **VSOP mit allen 9 Stellen rechnen**
   ✅ Ja. Intern mit `double`, aber aus dem präzise gelesenen JD. Kein `ToString("F9") → Parse`.

3. **Ergebnisse auf 9 Stellen trunkiert ins Ergebnis-JSON schreiben**
   ✅ Ja — für:

   * JD
   * Position
   * Velocity

4. **Delta-Ausgabe am Prompt auf 9 Stellen genau, analog TestEditor**
   ✅ Ja.

Die Präzisierung:

```text
Intern: volle double-Rechnung
Extern: 9 Stellen trunciert
```

Also nicht intern auf 9 Stellen beschneiden, sondern:

```text
Input JSON 9 decimals
→ parse zu double
→ VSOP rechnet mit double
→ Output formatter trunciert auf 9 decimals
```

Wichtig ist die harte Regel:

```text
Truncate only at output boundaries.
Never truncate inside the computation pipeline.
```

Damit ist die Zieldefinition sauber.


> Genau, und das ist now and forever die Verarbeitungs-Policy!

Perfekt. Und das ist eine wirklich gute Policy, weil sie:

* wissenschaftlich sauber
* deterministisch
* praktisch implementierbar
* und auditierbar

ist.

Der Kern ist:

```text
Internal Precision Policy
-------------------------
- internal computation:
  full IEEE754 double precision

- external persistence:
  fixed 9-decimal truncation

- truncation allowed only at output boundaries

- no formatted value may re-enter the computation pipeline
```

Das ist exakt die Trennung, die AstronoSphere braucht zwischen:

```text
Numerics
vs
Deterministic Serialization
```

Und wichtig:

```text
9 decimals = persistence contract
NOT
internal numerical limit
```

Damit verschwindet auch der gefährliche Graubereich, der heute die Verunsicherung erzeugt hat.

Ich würde diese Policy später wirklich als eigenes kleines Core-Dokument einfrieren, weil sie:

* Hashing
* SimulationData
* StateHash/DataHash
* Astronometria
* Astronolysis
* Trust-Cases

direkt beeinflusst.


> Ich habe den letzten Post bereits als Core-Dokument abgespeichert. Daraus machen wir später eine 100% Spec. Backlog: ## Precision Policy ausgehend von CORE_JD-PrecisionBackgroundAndAstronoPrecisionAudit.md und CORE_Precision+Rounding+TruncationPolicy.md ein echtes CanonicalPrecision-Core-Dokument erstellen.

Ja, das ist genau der richtige nächste Schritt.
Und ehrlich: das ist inzwischen kein „Nice-to-have“-Dokument mehr, sondern ein echtes Core-Thema.

Der Backlog-Eintrag ist sehr gut formuliert, weil er die drei Ebenen sauber trennt:

```text id="py6s2g"
1. Hintergrund / Motivation
2. konkrete Processing Policy
3. daraus abgeleitete Canonical Spec
```

Das spätere `CORE_CanonicalPrecision.md` sollte dann wirklich normativ werden:

```text id="0wx80m"
MUST
MUST NOT
SHALL
SHALL NOT
```

und explizit definieren:

* internal precision policy
* serialization policy
* truncation semantics
* rounding prohibition
* canonicalization precision
* hash precision
* delta precision
* replay guarantees
* precision-safe JD domain
* MXT2 warning semantics
* G17 roundtrip rules
* JSON persistence contract
* CSV raw preservation
* allowed / forbidden conversions

Das wird später eine der wichtigsten Vertrauens-Specs von AstronoSphere werden.


> Genau. Nun zurück zu unserem Astronometria Thema: was müssen wir ändern?

Wir müssen in Astronometria **nicht den VSOP-Algorithmus selbst ändern**, sondern die **Precision Policy an den Grenzen sauber durchsetzen**.

Konkret:

# Änderungen

1. **Experiment-Input**

   * JD aus Experiment-JSON mit voller vorhandener Präzision einlesen.
   * Kein String-Formatting vor der Rechnung.
   * Kein `ToString(...) → Parse(...)`.

2. **VSOP / Simulation**

   * intern weiter mit `double`.
   * keine Trunkierung im Rechenpfad.

3. **Simulation-JSON Output**

   * `JD`
   * `Position.X/Y/Z`
   * `Velocity.X/Y/Z`

   jeweils **auf exakt 9 Nachkommastellen trunciert** schreiben.

4. **Delta-Ausgabe am Prompt**

   * Deltas ebenfalls **trunciert auf 9 Nachkommastellen** ausgeben.
   * analog TestEditor.

5. **Zentraler Formatter**

   * Eine zentrale Funktion, z.B.:

```csharp
FormatTruncated9(double value)
```

Wichtig: nicht runden.

# Nicht ändern

* VSOP-Rechnung selbst
* double als interner Typ
* physikalische Logik
* StateVector-Berechnung

# Ich brauche als Nächstes

Bitte poste die ganzen Astronometria-Files, in denen:

* Experiment-JSON gelesen wird
* VSOP-StateVector erzeugt wird
* Simulation-JSON geschrieben wird
* Delta/Console-Ausgabe erzeugt wird

Dann liefere ich Dir One-File nach One-File den minimalen Fix.


> Kann ich Dir ein ZIP-Archiv schicken?

Ja, sehr gerne. Das ist hier sogar sinnvoller als viele Einzel-Files.

Am besten:

* ZIP mit den relevanten Astronometria-Projekten
* inkl. Pfadstruktur
* keine Binärordner nötig (`bin/`, `obj/` kannst Du weglassen)

Besonders wichtig:

* JSON Reader
* Simulation Writer
* VSOP Engine
* Delta/Validation Console
* Formatter/Utility Klassen

Dann analysiere ich:

1. wo Precision verloren gehen KÖNNTE
2. wo wir die 9-decimal truncation zentral einbauen sollten
3. wo Gefahr von accidental roundtrip besteht

und wir machen dann gezielt minimale Änderungen.


> ZIP Archives of Core and Desktop hochgeladen.

Ja, für den **Astronometria ScientificRun / Output-Fix** ist fast alles drin.

Gefunden:

* Experiment-Reader: `ExperimentLoader.cs`
* GroundTruth-Reader: `GroundTruthLoader.cs`
* Simulation-Executor: `ScientificRunSimulationExecutor.cs`
* Simulation-JSON Writer: `ScientificSimulationJsonWriter.cs`
* Delta-Konsole: `ScientificRunHost.cs`
* Hashing: `ScientificHashService.cs`

Für die aktuelle Precision-Policy müssen wir primär ändern:

1. `ScientificSimulationJsonWriter.cs`
   → Output auf **9 Stellen trunciert**, nicht gerundet.

2. `ScientificRunHost.cs`
   → Prompt-/Delta-Ausgabe ebenfalls trunciert auf 9 Stellen.

3. Optional später: gemeinsamer Formatter, damit Writer und Console dieselbe Logik verwenden.

Was **nicht** vollständig enthalten ist:

* die eigentliche VSOP-Engine:

  * `Astronometria.Ephemerides`
  * `VsopProvider`
  * `PlanetPositionService`
  * `TTInstant`
  * `StateVector`

Für Punkt 2 „VSOP Algo rechnet mit allen 9 Stellen“ sehe ich im Executor: `new TTInstant(gtSample.JD)` wird direkt aus `double` erzeugt, ohne String-Roundtrip. Das ist gut. Aber den tatsächlichen VSOP-Pfad kann ich ohne die Ephemerides-Files nicht vollständig auditieren.

Kurzfazit:

```text
Für Output + Delta Fix: ZIP reicht.
Für vollständigen VSOP Precision Audit: Ephemerides-Projekt fehlt.
```

Nächster sinnvoller Schritt: zuerst `ScientificSimulationJsonWriter.cs` fixen.

> Astronmetria_Ephemeris.zip hochgeladen

Sehr gut — jetzt kann ich Dir deutlich belastbarer antworten.

# Ergebnis des VSOP-Precision-Audits (vorläufig)

Ich habe gezielt gesucht nach:

```text
ToString(...)
double.Parse(...)
Math.Round(...)
GetRawText(...)
```

innerhalb des Ephemerides-/VSOP-Pfads.

## Befund

Gefunden wurden nur:

### 1. Repository/File-Namen

```csharp
id.ToString().ToLower()
```

→ irrelevant für Numerik.

### 2. VSOP87Parser

```csharp
double.Parse(...)
```

beim Einlesen der VSOP-Koeffizienten.

→ korrekt und notwendig.

---

# Wichtigste Erkenntnis

Ich sehe aktuell KEIN Muster wie:

```text
double
→ decimal string
→ parse zurück
→ weiterrechnen
```

im VSOP-Rechenpfad.

Das ist die entscheidende gute Nachricht.

---

# Bedeutet konkret

Aktueller Stand der Arbeitshypothese:

```text
VSOP intern rechnet mit voller double-Präzision.
```

und:

```text
Der 9-decimal Layer sitzt aktuell primär
bei Persistenz / Darstellung / Canonicalization.
```

Das ist exakt die gewünschte Architektur.

---

# Was jetzt noch offen bleibt

Wir haben noch NICHT geprüft:

* ScientificSimulationJsonWriter
* Delta Formatter
* evtl. StateVector Formatter
* evtl. spätere Astronolysis Exporte

Dort kann natürlich weiterhin:

* gerundet
* falsch formatiert
* inkonsistent trunciert

werden.

ABER:

```text
Das wäre ein Output-Problem,
nicht ein VSOP-Rechenproblem.
```

Das ist ein riesiger Unterschied.

---

# Mein aktuelles Gesamturteil

Mit den jetzt gesehenen Files würde ich sagen:

```text
Keine Hinweise auf stillen Precision-Loss
innerhalb der VSOP-Engine.
```

Und das passt auch zu Deinen extrem guten Validierungswerten.

---

# Damit ist die Architektur jetzt ziemlich klar

```text
Input JSON
(9 decimal persistence contract)
↓
double
↓
VSOP computation
(full double precision)
↓
Output Formatter
(9 decimal truncation policy)
↓
JSON / Console / Delta
```

Und genau DAS müssen wir jetzt sauber implementieren.


> Also, dann machen wir das jetzt.

Ich gehe jetzt gezielt in den Implementierungsteil: zuerst zentrale Stellen finden, dann maximal kleine File-Updates für Output-JSON und Delta-Konsole. Kein VSOP-Refactor.

-----------

Ja. Wir machen das jetzt in **zwei kleinen Schritten**.

Erster Schritt: **Simulation-JSON Output**.
Ich ändere nur den Writer, nicht die Rechnung.

---

# 📄 UPDATE

**Pfad:**
`04_Astronometria/src/Astronometria.Core/ScientificRun/IO/ScientificSimulationJsonWriter.cs`

**Status:** GEÄNDERT
**Ziel:** Output-Zahlen exakt **9 Nachkommastellen, trunciert**, keine Rundung.

```csharp
// ============================================================
// FILE: 04_Astronometria/src/Astronometria.Core/ScientificRun/IO/ScientificSimulationJsonWriter.cs
// STATUS: GEÄNDERT (Precision Policy – JSON output truncates to 9 decimals)
// ============================================================

using System;
using System.Globalization;
using System.IO;
using System.Text;
using System.Text.Encodings.Web;
using System.Text.Json;
using Astronometria.Core.ScientificRun.Models;

namespace Astronometria.Core.ScientificRun.IO
{
    public static class ScientificSimulationJsonWriter
    {
        private const decimal Scale9 = 1000000000m;

        public static void Write(string path, ScientificSimulationData data)
        {
            var json = BuildJson(data);
            File.WriteAllText(path, json, new UTF8Encoding(encoderShouldEmitUTF8Identifier: false));
        }

        private static string BuildJson(ScientificSimulationData data)
        {
            var sb = new StringBuilder();

            sb.AppendLine("{");
            AppendRunClassification(sb, data);
            AppendExperimentRef(sb, data);
            AppendMeasurement(sb, data);
            AppendGroundTruthRef(sb, data);
            AppendEngine(sb, data);
            AppendEngineCitation(sb, data);
            AppendProvenance(sb, data);
            AppendObservationScene(sb, data);
            sb.AppendLine("}");

            return sb.ToString();
        }

        private static void AppendRunClassification(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"RunClassification\": {");
            JsonProp(sb, 4, "RunType", data.RunClassification.RunType, comma: true);
            JsonProp(sb, 4, "InputType", data.RunClassification.InputType, comma: true);
            JsonProp(sb, 4, "TargetCardinality", data.RunClassification.TargetCardinality, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendExperimentRef(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"ExperimentRef\": {");
            JsonProp(sb, 4, "CatalogNumber", data.ExperimentRef.CatalogNumber, comma: true);
            JsonProp(sb, 4, "ExperimentID", data.ExperimentRef.ExperimentID, comma: true);
            JsonProp(sb, 4, "CoreHash", data.ExperimentRef.CoreHash, comma: true);
            JsonProp(sb, 4, "SourceFile", data.ExperimentRef.SourceFile, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendMeasurement(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"Measurement\": {");
            JsonProp(sb, 4, "Domain", data.Measurement.Domain, comma: true);
            JsonProp(sb, 4, "Instrument", data.Measurement.Instrument, comma: true);
            JsonProp(sb, 4, "CorrectionLevel", data.Measurement.CorrectionLevel, comma: true);
            JsonProp(sb, 4, "TimeScale", data.Measurement.TimeScale, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendGroundTruthRef(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"GroundTruthRef\": {");
            JsonProp(sb, 4, "Provider", data.GroundTruthRef.Provider, comma: true);
            JsonProp(sb, 4, "DatasetID", data.GroundTruthRef.DatasetID, comma: true);
            JsonProp(sb, 4, "RequestHash", data.GroundTruthRef.RequestHash, comma: true);
            JsonProp(sb, 4, "SourceFile", data.GroundTruthRef.SourceFile, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendEngine(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"Engine\": {");
            JsonProp(sb, 4, "Name", data.Engine.Name, comma: true);
            sb.AppendLine("    \"SimulationModel\": {");
            JsonProp(sb, 6, "Family", data.Engine.SimulationModel.Family, comma: true);
            JsonProp(sb, 6, "Type", data.Engine.SimulationModel.Type, comma: true);
            JsonProp(sb, 6, "Truncation", data.Engine.SimulationModel.Truncation, comma: true);
            JsonProp(sb, 6, "TimeDomain", data.Engine.SimulationModel.TimeDomain, comma: false);
            sb.AppendLine("    },");
            sb.AppendLine("    \"Build\": {");
            JsonProp(sb, 6, "GitCommit", data.Engine.Build.GitCommit, comma: true);
            JsonProp(sb, 6, "GitBranch", data.Engine.Build.GitBranch, comma: false);
            sb.AppendLine("    }");
            sb.AppendLine("  },");
        }

        private static void AppendEngineCitation(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"EngineCitation\": {");
            JsonProp(sb, 4, "Provider", data.EngineCitation.Provider, comma: true);
            JsonProp(sb, 4, "Source", data.EngineCitation.Source, comma: true);
            JsonProp(sb, 4, "Citation", data.EngineCitation.Citation, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendProvenance(StringBuilder sb, ScientificSimulationData data)
        {
            sb.AppendLine("  \"Provenance\": {");
            JsonProp(sb, 4, "ExperimentFactory", data.Provenance.ExperimentFactory, comma: true);
            JsonProp(sb, 4, "TruthFactory", data.Provenance.TruthFactory, comma: true);
            JsonProp(sb, 4, "SimulationEngine", data.Provenance.SimulationEngine, comma: false);
            sb.AppendLine("  },");
        }

        private static void AppendObservationScene(StringBuilder sb, ScientificSimulationData data)
        {
            var scene = data.ObservationScene;
            var targetSimulation = scene.TargetSimulations[0];

            sb.AppendLine("  \"ObservationScene\": {");
            sb.AppendLine("    \"SceneContext\": {");
            JsonProp(sb, 6, "TemporalMode", scene.SceneContext.TemporalMode, comma: true);

            sb.AppendLine("      \"Time\": {");
            JsonNumberProp(sb, 8, "StartJD", scene.SceneContext.Time.StartJD, comma: true);
            JsonNumberProp(sb, 8, "StopJD", scene.SceneContext.Time.StopJD, comma: true);
            JsonProp(sb, 8, "Step", scene.SceneContext.Time.Step, comma: true);
            JsonProp(sb, 8, "TimeScale", scene.SceneContext.Time.TimeScale, comma: false);
            sb.AppendLine("      },");

            sb.AppendLine("      \"Observer\": {");
            JsonProp(sb, 8, "Type", scene.SceneContext.Observer.Type, comma: true);
            JsonProp(sb, 8, "Body", scene.SceneContext.Observer.Body, comma: false);
            sb.AppendLine("      },");

            sb.AppendLine("      \"Frame\": {");
            JsonProp(sb, 8, "Origin", scene.SceneContext.Frame.Origin, comma: true);
            JsonProp(sb, 8, "Plane", scene.SceneContext.Frame.Plane, comma: true);
            JsonProp(sb, 8, "Type", scene.SceneContext.Frame.Type, comma: true);
            JsonProp(sb, 8, "Epoch", scene.SceneContext.Frame.Epoch, comma: true);
            JsonProp(sb, 8, "RefSystem", scene.SceneContext.Frame.RefSystem, comma: false);
            sb.AppendLine("      }");
            sb.AppendLine("    },");

            sb.AppendLine("    \"TargetSimulations\": [");
            sb.AppendLine("      {");
            sb.AppendLine("        \"Target\": {");
            JsonProp(sb, 10, "BodyClass", targetSimulation.Target.BodyClass, comma: true);
            JsonProp(sb, 10, "Name", targetSimulation.Target.Name, comma: true);
            JsonProp(sb, 10, "Abbreviation", targetSimulation.Target.Abbreviation, comma: false);
            sb.AppendLine("        },");

            sb.AppendLine("        \"TerminalNode\": {");
            JsonProp(sb, 10, "NodeId", targetSimulation.TerminalNode.NodeId, comma: true);
            JsonProp(sb, 10, "NodeType", targetSimulation.TerminalNode.NodeType, comma: true);
            JsonProp(sb, 10, "NodeRole", targetSimulation.TerminalNode.NodeRole, comma: true);
            JsonProp(sb, 10, "Status", targetSimulation.TerminalNode.Status, comma: true);
            JsonProp(sb, 10, "StateHash", targetSimulation.TerminalNode.StateHash, comma: true);
            JsonProp(sb, 10, "DataHash", targetSimulation.TerminalNode.DataHash, comma: true);
            sb.AppendLine("          \"Data\": [");

            for (var i = 0; i < targetSimulation.TerminalNode.Data.Count; i++)
            {
                var sample = targetSimulation.TerminalNode.Data[i];
                var comma = i < targetSimulation.TerminalNode.Data.Count - 1 ? "," : string.Empty;

                sb.AppendLine("            {");
                sb.AppendLine($"              \"JD\": {FormatTruncated9(sample.JD)},");
                sb.AppendLine($"              \"Position\": {{\"X\":{FormatTruncated9(sample.Position.X)},\"Y\":{FormatTruncated9(sample.Position.Y)},\"Z\":{FormatTruncated9(sample.Position.Z)}}},");
                sb.AppendLine($"              \"Velocity\": {{\"X\":{FormatTruncated9(sample.Velocity.X)},\"Y\":{FormatTruncated9(sample.Velocity.Y)},\"Z\":{FormatTruncated9(sample.Velocity.Z)}}}");
                sb.AppendLine($"            }}{comma}");
            }

            sb.AppendLine("          ]");
            sb.AppendLine("        }");
            sb.AppendLine("      }");
            sb.AppendLine("    ]");
            sb.AppendLine("  }");
        }

        private static void JsonProp(StringBuilder sb, int indent, string name, string value, bool comma)
        {
            var options = new JsonSerializerOptions
            {
                Encoder = JavaScriptEncoder.UnsafeRelaxedJsonEscaping
            };

            sb.Append(' ', indent);
            sb.Append("\"");
            sb.Append(name);
            sb.Append("\": ");
            sb.Append(JsonSerializer.Serialize(value, options));
            sb.AppendLine(comma ? "," : string.Empty);
        }

        private static void JsonNumberProp(StringBuilder sb, int indent, string name, double value, bool comma)
        {
            sb.Append(' ', indent);
            sb.Append("\"");
            sb.Append(name);
            sb.Append("\": ");
            sb.Append(FormatTruncated9(value));
            sb.AppendLine(comma ? "," : string.Empty);
        }

        private static string FormatTruncated9(double value)
        {
            var truncated = Math.Truncate((decimal)value * Scale9) / Scale9;
            return truncated.ToString("0.000000000", CultureInfo.InvariantCulture);
        }
    }
}
```

---

Wichtige Änderung nebenbei: Velocity wird jetzt nicht mehr hart als `0.0` geschrieben, sondern aus `sample.Velocity` formatiert. Aktuell bleibt das faktisch `0.000000000`, ist aber sauberer.

Danach kommt Schritt 2: `ScientificRunHost.cs` für die Delta-/Prompt-Ausgabe.
