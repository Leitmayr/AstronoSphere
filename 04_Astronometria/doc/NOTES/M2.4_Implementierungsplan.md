## Implementierungsplan M2.4.0

### WP1 — StateTree-Grundtypen

**NEU**

```text
ScientificRun/StateTree/PhysicsStateNodeType.cs
ScientificRun/StateTree/PhysicsStateTreePath.cs
```

Ziel: interne `PHYS.*` NodeTypes und geordnete Pfade modellieren.

---

### WP2 — StateTreeRegistry

**NEU**

```text
ScientificRun/StateTree/PhysicsStateTreeRegistry.cs
```

Ziel: genau drei erlaubte M2.4.0-Pfade:

```text
HELIO-ECL → PHYS.L0.HELIO.ECL.J2000.VEC
GEO-ECL   → PHYS.L0.HELIO.ECL.J2000.VEC → PHYS.L0.GEO.ECL.J2000.VEC
GEO-EQU   → PHYS.L0.HELIO.ECL.J2000.VEC → PHYS.L0.GEO.ECL.J2000.VEC → PHYS.L0.GEO.EQU.J2000.VEC
```

---

### WP3 — MeasurementDefinition Mapping

**NEU**

```text
ScientificRun/StateTree/MeasurementDefinitionToNodeTypeMapper.cs
```

Ziel: `MeasurementDefinition` → interner TerminalNodeType `PHYS.*`.

Fehlerfälle später über Diagnostics prüfen.

---

### WP4 — TerminalNodeDeriver anpassen

**ERWEITERUNG**

```text
ScientificRun/Planning/TerminalNodeDeriver.cs
```

Ziel: intern `PHYS.*` ableiten, aber persistierte JSON-NodeType-Ausgabe noch Legacy-kompatibel halten.

---

### WP5 — StateTreeExecutor

**NEU**

```text
ScientificRun/StateTree/PhysicsStateTreeExecutor.cs
```

Ziel: Ordered Path ausführen, zunächst KISS:

```text
HELIO-ECL compute
GEO-ECL transform
GEO-EQU rotate
```

Keine Physikänderung.

---

### WP6 — ScientificRunSimulationExecutor integrieren

**ERWEITERUNG**

```text
ScientificRun/Execution/ScientificRunSimulationExecutor.cs
```

Ziel: nicht mehr direkt nach Frame rechnen, sondern:

```text
MeasurementDefinition
→ PHYS TerminalNodeType
→ Registry Path
→ Executor
→ Terminal Data
```

---

### WP7 — Hashing prüfen/anpassen

**ERWEITERUNG**

```text
ScientificRun/Hashing/*
```

Ziel: StateHash/DataHash nach aktueller Spec stabil halten. Keine unnötige Hash-Migration.

---

### WP8 — Diagnostics

**ERWEITERUNG**

```text
ScientificRun/Diagnostics/*
```

Ziel: falls noch nicht vorhanden:

```text
040.010 Unknown MeasurementDefinition
040.011 Unknown NodeType
```

Nur wenn im Codepfad sauber integrierbar, sonst M2.4.0 minimal halten.

---

### WP9 — Validierung

Reihenfolge:

```text
1. Build
2. Existing HELIO-ECL laufen lassen
3. Existing GEO-ECL laufen lassen
4. Payload gegen M2.3 plausibilisieren
5. GEO-EQU neu laufen lassen
6. Run erneut laufen lassen
7. Run == LastRun prüfen
```

## Startpunkt nächstes Mal

Mit **WP1 + WP2** beginnen. Danach erst Integration. Viel Spaß beim Sport.


Das Gefühl hatte ich auch. Die ersten paar Posts waren noch "Wo waren wir stehen geblieben?", aber spätestens bei der PHYS/VSOP-Diskussion waren wir wieder voll im AstronoSphere-Modus. 😊

Vor allem haben wir heute zwei wichtige Architekturentscheidungen getroffen:

1. **PHYS intern, Legacy persistiert**

   * verhindert Baseline-Chaos
   * hält M2.4 validierbar

2. **Kapitel 0 Current Milestone Freeze**

   * macht die StateMachine-Spec langfristig wartbar
   * trennt Architektur von Implementierungsstand

Beides sind typische Entscheidungen, die später enorm viel Ärger sparen.

---

# Aufwandsschätzung M2.4.0

Unter der Annahme:

* bestehender Code = unverändert
* keine Überraschungen im GEO-EQU Zweig
* keine größeren Spec-Änderungen mehr
* STRICT Mode

| WP  | Inhalt                               | Aufwand |
| --- | ------------------------------------ | ------: |
| WP1 | StateNodeType + Path Klassen         |   0.5 h |
| WP2 | StateTreeRegistry                    |   0.5 h |
| WP3 | MeasurementDefinition → PHYS Mapping |   0.5 h |
| WP4 | TerminalNodeDeriver Umbau            |   0.5 h |
| WP5 | PhysicsStateTreeExecutor             |   1.0 h |
| WP6 | Executor Integration                 |   1.0 h |
| WP7 | Hashing Anpassungen                  |   0.5 h |
| WP8 | Diagnostics                          |   0.5 h |
| WP9 | Validierung + Compare                |   2.0 h |

---

## Gesamt

### Optimistisch

```text
~5 Stunden
```

### Realistisch

```text
~7 Stunden
```

### Mit Puffer

```text
~8-10 Stunden
```

---

## Meine heutige Einschätzung

Vor dem Urlaub hätte ich gesagt:

```text
10-12 Stunden
```

Heute würde ich deutlich niedriger schätzen:

```text
6-8 Stunden
```

Warum?

Die eigentliche Architekturarbeit ist erledigt.

Heute haben wir:

```text
MeasurementDefinition
↓
PHYS NodeType
↓
StateTreeRegistry
↓
Path
↓
Executor
```

glasklar definiert.

Das ist keine Forschungsarbeit mehr.

Das ist jetzt ziemlich klassische Implementierung.

---

## Feierabend-Sessions

In Deiner typischen Arbeitsweise:

```text
Session 1
WP1-WP4

Session 2
WP5-WP6

Session 3
WP7-WP8 + erste Tests

Session 4
WP9 Validierung + Baseline
```

Also ziemlich genau:

> **4 Feierabend-Sessions**

Interessanterweise ist das exakt dieselbe Größenordnung, die wir vor dem Urlaub bereits vermutet hatten. Das ist immer ein gutes Zeichen: Die Architektur hat sich stabilisiert und die Aufwandsabschätzung läuft nicht mehr weg.

